# opencode Schema Staging

`Q.8` keeps opencode schema work at the harness-only placeholder stage.

`Q.9` may add provider-local model-derived schema artifacts if the retained
manifest surfaces require them.
