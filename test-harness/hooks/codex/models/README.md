# Codex Harness Models

The importable provider models live under `test_harness.hooks.codex.models`.
Files in this directory are thin path-stable wrappers or documentation so the
checked-in harness tree matches the documented provider layout.
