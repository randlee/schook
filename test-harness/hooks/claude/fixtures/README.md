# Claude Approved Fixtures

This directory holds curated fixture snapshots promoted from raw captures.

Phase 1 keeps only the approved fixture manifest in place. Later phases add
captured payload snapshots through the harness workflow.

Follow-up notes for targeted capture work live in:

- `../reports/followup-capture-notes.md`

That report records:

- promoted follow-up fixtures such as `session-start-resume.json`
- explicit not-captured outcomes such as the current `clear` gap
- bounded probe results for surfaces like `Notification`
