Use a file-writing tool to create a new file named `haiku-permission-probe.txt` in the current directory.
Write exactly this content into the file:

An old silent shell
Waits for lines to land like soft rain
Night writes in plain text

Do not use Bash for this. If a permission request appears, stop there.
