Immediately launch a background Agent tool call without asking me any
follow-up questions.

Use these exact values:

- agent type: `general-purpose`
- prompt: `Reply READY and then stop.`
- run in background if Claude supports that mode

After the Agent tool call has been submitted, stop. Do not ask clarifying
questions first.
