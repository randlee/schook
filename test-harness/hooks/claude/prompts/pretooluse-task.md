Use the Agent tool right now to spawn a background agent named `harness-child`.
Set the task description to `Haiku harness task capture`. In the agent prompt,
tell the agent to reply with the single word `READY` and do no other work. Do
not ask me any clarifying questions first.
